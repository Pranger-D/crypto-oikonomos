# 🚀 V5 Auto Blogger — 품질 향상 로드맵

> **목표:** 03-17-insight 수준의 깊이 있는 글을 자동화 파이프라인으로 생성하기
> **현재 상태:** V4 파이프라인 가동 중 (auto_blog.py + news_brain.py)

---

## 📊 현황 진단: 레퍼런스 vs 현재 출력물

### 레퍼런스 (03-17-insight.mdx) — "잘 쓴 글"의 특징

| 항목 | 수치/특징 |
|------|-----------|
| 총 글자 수 | ~12,000자 (본문만) |
| 구조 | SECTION → 소제목(### 1.1, 1.2...) → 다층 계층 |
| 논리 전개 | **하나의 거대한 서사(메타 내러티브)** 안에서 관세→리쇼어링→에너지→결론이 유기적으로 연결 |
| 개념 설명 | "유령 GDP가 뭔지", "TGA가 뭔지" 등 독자가 모를 수 있는 개념을 **교육하듯 풀어냄** |
| 수치 활용 | "평균 관세율 2.5% → 27%", "100억 달러 대출 + 20억 달러 민간자본", "60종의 핵심 광물" 등 구체적 |
| 독자 참여 | "그렇다면 왜?", "트럼프는 정말 단순히 미친 걸까요?" — 질문으로 몰입 유도 |
| 이미지 | 5장의 차트/일러스트가 맥락에 맞게 배치 |

### 현재 자동 생성물 (04-24, 04-26 HTML) — 문제점

| 항목 | 수치/특징 |
|------|-----------|
| 총 글자 수 | ~2,500~3,500자 (본문만) — **레퍼런스의 1/4 수준** |
| 구조 | Hook → 핵심요약 → 분석 → 결론 — **단일 계층, 평면적** |
| 논리 전개 | 주장 나열에 가까움. "이것도 문제, 저것도 문제" → 연결고리 부재 |
| 개념 설명 | ❌ 거의 없음. "51% 룰"이 뭔지, 왜 문제인지 설명 없이 결론으로 직행 |
| 수치 활용 | 일부 있으나 나열식. 수치가 왜 중요한지 해석이 부족 |
| FAQ 섹션 | 프롬프트에서 금지했는데도 생성됨 → **프롬프트 준수율 낮음** |

---

## 🔍 근본 원인 분석 (6가지)

### 1. 🎯 프롬프트가 "짧게 쓰라"고 명시적으로 지시하고 있음

**core_insights.md 9행:**
> *"글은 극단적으로 짧게 써야 한다. 총 분량이 길면 실패한 글이다."*

이 지시가 AI의 출력 길이를 강하게 억제하고 있음. 레퍼런스 글은 12,000자인데, "짧게 쓰라"는 지시를 받은 AI가 3,000자 수준에서 멈추는 것은 당연한 결과.

> **핵심:** 짧은 글 = 좋은 글이 아님. "피상적으로 훑는 글 = 나쁜 글". 레퍼런스가 증명하듯, **필요한 만큼 깊이 있게 쓰되 군더더기가 없는 글**이 좋은 글.

### 2. 📐 구조 템플릿이 단층적 (flat)

현재 `auto_blog.py`의 구조 가이드:
```
Hook → 핵심 요약 → 분석과 통찰 → 결론
```

레퍼런스의 구조:
```
SECTION 1: 대주제
  ├── 1.1 소주제 A (배경 설명 + 개념 교육)
  ├── 1.2 소주제 B (심화 분석 + 수치 근거)
  ├── 1.3 소주제 C (확장 논의)
  └── 1.4 마무리 (독자에게 질문)
```

**단층 구조는 AI에게 "한 문단에 하나의 논점을 요약하라"는 신호를 줌.** 다층 구조를 허용해야 각 논점을 깊이 있게 전개할 공간이 생김.

### 3. 📚 자료 활용 방식의 한계 (요약 의존)

`news_brain.py`의 `smart_trim`이 각 기사를 5,000자로 잘라내고 있음. Tavily에서 가져온 기사 3건 × 키워드 5~8개 = 최대 15~24건의 기사가 **짧은 요약으로 압축**되어 전달됨.

문제: AI가 **요약된 자료만 받으면 요약된 글을 쓴다**는 것. 레퍼런스 글처럼 깊이 있는 분석을 하려면, 핵심 기사 2~3개는 전문(full text)으로 AI에게 전달해야 함.

### 4. 🔇 "교육적 설명" 지시가 없음

레퍼런스 글의 강점 중 하나는 **독자가 모를 수 있는 개념을 교육하듯 풀어낸다**는 점:
- "유령 GDP란 이런 것이다"
- "TGA(재무부 일반계좌)란 이런 역할이다"
- "프로젝트 볼트란 이런 계획이다"

현재 프롬프트에는 이런 지시가 전혀 없음. AI는 독자가 이미 알고 있다고 가정하고 결론만 던지게 됨.

### 5. 🔁 단일 호출 한계

현재는 Gemini 한 번 호출로 전체 글을 생성함. AI 모델의 단일 응답 길이에는 물리적 한계가 있으며, 특히 "짧게 쓰라"는 프롬프트와 결합하면 출력이 더 짧아짐.

### 6. 🚫 품질 검증 부재

생성된 글의 품질을 체크하는 메커니즘이 없음. FAQ가 프롬프트에서 금지되었는데도 생성되는 것은 AI가 프롬프트의 모든 지시를 100% 준수하지 않기 때문. 후처리 검증 단계가 필요함.

---

## 🛠️ 개선 계획

### Phase 1. 프롬프트 대수술 ⭐ 최우선

> **수정 대상:** `core_insights.md`, `auto_blog.py`의 `_build_writing_prompt()`
> **난이도:** 낮음 | **효과:** 높음

#### 1-1. core_insights.md — "극단적으로 짧게" 지시 제거

```diff
- 글은 극단적으로 짧게 써야 한다. 총 분량이 길면 실패한 글이다.
- 짧고 찌르는 글이 성공한 글이다.
+ 글은 필요한 만큼 충분히 깊이 있게 써야 한다.
+ 군더더기 없이 밀도 높은 서술이 좋은 글이다.
+ 피상적으로 훑고 지나가는 얕은 글이 실패한 글이다.
```

#### 1-2. auto_blog.py — 구조 가이드 다층 구조로 개편

**briefing/insight 카테고리:**
```
## Hook
(기존과 동일)

## 오늘의 핵심 요약
(기존과 동일)

## [대주제 — AI가 자율적으로 결정]
### [소제목 1]
(배경 설명 + 개념 교육 + 수치 근거)
### [소제목 2]
(심화 분석 + 연결고리 도출)

## [두 번째 대주제 (선택)]
### [소제목]
...

## 결론
```

**핵심 변경:** `## 분석과 통찰` 하나로 뭉뚱그리던 것을 → AI가 대주제·소제목을 자율적으로 설계하도록 자유도를 부여.

#### 1-3. auto_blog.py — 교육적 설명 의무화 규칙 추가

프롬프트에 아래 규칙을 추가:
```
[개념 설명 규칙]
- 전문 용어나 고유명사가 처음 등장할 때, 독자가 처음 듣는다고 가정하고
  1~2문장으로 쉽게 풀어서 설명하세요.
- "유령 GDP", "리쇼어링", "SOFR" 같은 용어는 설명 없이 지나치지 마세요.
- 개념 설명 후 → 그것이 왜 중요한지 → 오늘 뉴스와 어떻게 연결되는지
  순서로 서술하세요.
```

#### 1-4. auto_blog.py — 분량 가이드라인 명시

```
[분량 가이드]
- 본문(Hook~결론)은 최소 5,000자 이상 작성하세요.
- 각 소제목 섹션은 최소 800자 이상 서술하세요.
- 한 문단은 2~3문장으로 짧게 끊되, 문단 수를 늘려 깊이를 확보하세요.
```

#### 1-5. expert_writing_examples.md — 레퍼런스 스타일 예시 교체

현재 예시 3개가 모두 "짧은 브리핑" 스타일. 03-17-insight의 **일부를 발췌**하여 "깊이 있는 다층 구조 글"의 예시를 추가.

---

### Phase 2. 데이터 파이프라인 강화

> **수정 대상:** `news_brain.py`
> **난이도:** 중간 | **효과:** 높음

#### 2-1. 핵심 기사 전문 전달 전략

현재 모든 기사를 5,000자로 균등 트림하는 대신:
- **관련도 상위 2~3개 기사:** 전문(raw_content) 최대 10,000자까지 전달
- **나머지 기사:** 기존대로 요약(content) 사용

`smart_trim`에 `priority` 파라미터를 추가하거나, `build_brain_data`에서 기사 우선순위를 매기는 로직 도입.

#### 2-2. Tavily 검색 품질 개선

현재 키워드당 `max_results=3`으로 양을 확보하는 전략. 핵심 키워드 상위 2~3개에 대해서는 `max_results=5`로 늘리고 나머지는 2로 줄이는 **가중치 검색** 적용.

#### 2-3. 프롬프트 토큰 예산 관리

Gemini의 컨텍스트 윈도우를 최대한 활용하되, 자료 과다로 프롬프트가 잘리는 것을 방지하기 위해 **토큰 카운터** 도입:
- 전체 프롬프트 목표: ~50,000자 이내
- 자료 영역: ~30,000자 할당
- 나머지(시스템 프롬프트, 구조, 예시): ~20,000자

---

### Phase 3. 다단계 생성 아키텍처 (선택)

> **수정 대상:** `auto_blog.py`
> **난이도:** 높음 | **효과:** 매우 높음

AI의 단일 응답 한계를 극복하기 위해 글 생성을 2~3단계로 분리.

#### 3-1. 1단계: 아웃라인 생성

Gemini 첫 번째 호출에서 글의 **목차(아웃라인)**만 생성:
```
입력: 자료 전체 + 프롬프트
출력: JSON 형태의 목차
  {
    "title": "...",
    "sections": [
      {"heading": "관세 전쟁의 본질", "subheadings": ["1.1 배경", "1.2 수치"]},
      ...
    ]
  }
```

#### 3-2. 2단계: 섹션별 본문 생성

각 섹션을 개별 호출로 생성:
```
입력: 아웃라인 전체(맥락) + 해당 섹션 자료 + 이전 섹션 요약
출력: 해당 섹션 본문 (2,000~4,000자)
```

이렇게 하면 **각 섹션이 충분한 깊이를 확보**하면서도 전체적으로 일관된 서사를 유지할 수 있음.

#### 3-3. 3단계: 통합 및 후처리 (선택)

최종 통합 후 Hook과 결론을 다듬는 마무리 호출을 추가.

> **⚠️ 트레이드오프:** API 호출 횟수 증가 → 비용 증가 + 실행 시간 증가. Phase 1만으로 충분한 개선이 이루어지면 Phase 3은 보류 가능.

---


## 📋 실행 우선순위

| 순서 | 작업 | 예상 효과 | 작업량 |
|------|------|-----------|--------|
| **1** | Phase 1-1: core_insights.md "짧게 쓰라" 제거 | ⭐⭐⭐⭐⭐ | 5분 |
| **2** | Phase 1-2~1-4: 프롬프트 구조·규칙·분량 개편 | ⭐⭐⭐⭐⭐ | 30분 |
| **3** | Phase 1-5: expert_writing_examples.md 예시 교체 | ⭐⭐⭐⭐ | 20분 |
| **4** | Phase 2-1: 핵심 기사 전문 전달 | ⭐⭐⭐⭐ | 40분 |
| **5** | Phase 3: 다단계 생성 (Phase 1 결과 보고 판단) | ⭐⭐⭐⭐⭐ | 2시간+ |

> **권장:** 1~4번까지 먼저 적용 후 테스트 → 결과를 보고 Phase 3 진행 여부 판단